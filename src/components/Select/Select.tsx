import {FunctionComponent, memo} from 'react'
import {Box, ContainerProps, SelectProps} from '@chakra-ui/react'

function Select : FunctionComponent<SelectProps> = () =>{
	return <Box>Select</Box>
}

export default memo(Select)
